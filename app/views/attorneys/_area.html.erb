<%= content_tag :p, area.name %>
<% if area.specialities.any? %>
<ul>
<% lawyer.practice_areas.child_practice_areas.where(parent_id: area.id).order("name").each do |spec| %>
  <%= content_tag :li, spec.name %>
<% end %>
</ul>
<% end %>
