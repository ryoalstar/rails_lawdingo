<%= simple_form_for [@lawyer, @offering] do |f| %>
  <%= f.input :practice_area_id, collection: PracticeArea.parent_practice_areas, include_blank: false, label: "Area offering belongs to:" %>
  <%= f.input :name, label: "Name of fixed-price offer:", hint: "e.g. \"Contract review — up to 3 pages\".".html_safe%>
  <%= f.input :description, label: "What specifically will you do and deliver?" %>
  <%= f.input :fee, label: "Fee typically charged:", hint: "The typical amount you charge for this.", as: :currency %>
  <%= f.submit params[:action] == "edit" ? "Edit fixed-price offer" : "Add fixed-price offer", class: "button", style: "font-size: 1.0833em;" %>
<% end %>
