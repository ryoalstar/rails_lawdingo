<div class="signup" id="page">
  <h1 class="form-header">Log In</h1>
  <div class="form-wrapper">
  <%= form_tag sessions_path, :validate => true do %>
    <div class="form-inner">
      <% if @msg %>
        <div class="error_explanation">
          <h4 class="error">Incorrect email or password.</h4>
        </div>
      <% end %>

      <%= text_field_tag :email, params[:email], :placeholder=>"Email address" ,:class=>"email" %>
      <%= password_field_tag :password ,nil ,:placeholder=>"Password" ,:class=>"passw" %>
    </div>

    <div class="actions" style="margin: 0 !important;">
      <div class="form-inner" style="padding: 20px 45px;">
        <%= submit_tag 'Log In' ,:class=>"button" %>
      </div>
    </div>
  <% end %>
  </div>

  <div class="form-wrapper signin">
    <p>New to Lawdingo? <%= link_to "Sign up", new_user_path(ut: 0) %> now.</p>
  </div>
</div>
